// https://on.cypress.io/configuration
// Cypress configuration
// Add your custom commands or overrides here

beforeEach(() => {
  // Add any setup needed before each test
})

afterEach(() => {
  // Add any cleanup needed after each test
})
